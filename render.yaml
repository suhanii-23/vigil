services:
  - type: web
    name: vigil-backend
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /videos
    envVars:
      - key: CORS_ORIGINS
        value: https://frontend-ashen-gamma-60.vercel.app
